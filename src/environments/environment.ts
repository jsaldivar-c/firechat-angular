// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebaseConfig: {
        apiKey: 'AIzaSyALahFK7BL5ejX4EifLRSmeoGoSsOu_DtI',
        authDomain: 'firechat-angular-8daec.firebaseapp.com',
        projectId: 'firechat-angular-8daec',
        storageBucket: 'firechat-angular-8daec.appspot.com',
        messagingSenderId: '773472696837',
        appId: '1:773472696837:web:4294b20dc43eb1e88c3a6c',
    },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
